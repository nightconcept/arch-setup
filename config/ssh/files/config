# Instruct the SSH-agent to automatically add the keys for all connections
AddKeysToAgent yes


# Add details for frequently used remote servers:

# Host {SERVER_ALIAS}
#    Hostname {SERVER_IP}
#    Port {PORT} # Optional, defaults to 22
#    User {SERVER_USER} # Optional, in case the client and server use different users
#    IdentityFile "~/.ssh/{PRIVATE_KEY}" # (only required if you have more than one key or if the key has a custom name)

Host macbook
    Hostname 192.168.100.24

Host xps
    Hostname 192.168.100.40

Host nuc
    Hostname 192.168.100.100

