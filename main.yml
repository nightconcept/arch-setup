# must be `all` and not `localhost` to be able to get the {{inventory_hostname}} value
- hosts: localhost

  # TODO
  # fnm
  # pnpm
  # cpu-x
  # hexchat
  # obsidian
  # vscode
  # wezterm
  # pyenv
  roles:
    # core system setup
    - pre_install
    - pacman
    - zsh
    - yay
    - common
    - system
    - kde
    - wayland
    - bluetooth
    - fonts
    # dotfiles

    # applications
    - mpv
    